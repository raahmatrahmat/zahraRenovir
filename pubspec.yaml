0

M-\   # untuk lompat ke awal file
M-/   # untuk lompat ke akhir file

name: zahra_renovir
description: Aplikasi Flutter ZahraRenovir untuk build APK otomatis.
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
0


0

